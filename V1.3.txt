版本说明：
1、从登录页面到主页面进行动画跳转
2、修改主页面动画细节问题，通过定时器方式解决鼠标不动动画还在动的问题。
3、解决鼠标不动动画还动的问题$(this).stop().animate $(this).find(".caption").fadeIn(300);// 渐现
        $(this).find(".menu").stop().fadeOut(300);

加stop（）；
4、解决系统管理与图标移动不同步的问题，统一向左移动